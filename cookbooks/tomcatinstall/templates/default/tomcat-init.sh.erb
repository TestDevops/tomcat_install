#!/bin/bash  
# description: Tomcat Start Stop Restart  
# processname: tomcat

CATALINA_HOME=<%= ::File.join(@home_dir, @service_name) %>

case $1 in  
  start)  
    sh $CATALINA_HOME/bin/startup.sh  
    ;;   
stop)     
    sh $CATALINA_HOME/bin/shutdown.sh  
    ;;   
restart)  
    sh $CATALINA_HOME/bin/shutdown.sh  
    sh $CATALINA_HOME/bin/startup.sh  
    ;;   
esac      
exit 0 